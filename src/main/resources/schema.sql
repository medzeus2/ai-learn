CREATE TABLE IF NOT EXISTS t_person (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    name_pinyin VARCHAR(120) NOT NULL DEFAULT '',
    name_initials VARCHAR(50) NOT NULL DEFAULT '',
    gender VARCHAR(20) NOT NULL,
    age INT NOT NULL,
    id_card VARCHAR(18) NOT NULL UNIQUE,
    status VARCHAR(20) NOT NULL
);

ALTER TABLE t_person ADD COLUMN IF NOT EXISTS name_pinyin VARCHAR(120) NOT NULL DEFAULT '';
ALTER TABLE t_person ADD COLUMN IF NOT EXISTS name_initials VARCHAR(50) NOT NULL DEFAULT '';
